[package]
name = "ahoy_replicon"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "lib.rs"

[dependencies]
bevy = { version = "0.18.1", features = ["serialize"] }
serde = { version = "1.0", features = ["derive"] }
